# Environment Configuration (copy to .env and fill in values)

# Server port
PORT=5000

# Node environment (development|production)
NODE_ENV=development

# JWT secret key (REQUIRED in production, change from default!)
JWT_SECRET=your-secure-random-secret-key-here

# Database file path (optional, defaults to db.json in backend folder)
DB_FILE=./db.json

# CORS origin (optional, defaults to * in development)
CORS_ORIGIN=*
